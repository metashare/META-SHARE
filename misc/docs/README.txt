The documentation and manuals can be generated using Sphinx:
http://sphinx-doc.org

Installation
============

From Debian/Ubuntu repositories:

$ apt-get install python-sphinx

Or with pip:

pip install sphinx

Build documentation
====================

- In HTML format:
   $ make html
- In PDF format (requires LaTeX):
   $ make latexpdf

